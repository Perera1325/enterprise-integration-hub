<?xml version="1.0" encoding="UTF-8"?>
<sequence name="CheckBusinessRulesSeq" trace="disable" xmlns="http://ws.apache.org/ns/synapse">
    
    <!-- Extract age calculation (mock implementation for age verification) -->
    <!-- In a real scenario, we might use a Script Mediator (JavaScript) to calculate age from DOB -->
    <script language="js"><![CDATA[
        var dob = mc.getProperty("ReqDateOfBirth");
        if (dob) {
            var birthDate = new Date(dob);
            var today = new Date();
            var age = today.getFullYear() - birthDate.getFullYear();
            var m = today.getMonth() - birthDate.getMonth();
            if (m < 0 || (m === 0 && today.getDate() < birthDate.getDate())) {
                age--;
            }
            mc.setProperty("CustomerAge", age);
        }
    ]]></script>
    
    <filter xpath="get-property('CustomerAge') &lt; 18">
        <then>
            <!-- Business Rule Violation -->
            <property name="ERROR_CODE" value="42201"/>
            <property name="ERROR_MESSAGE" value="Unprocessable Entity"/>
            <property name="ERROR_DETAIL" value="Customer must be at least 18 years old"/>
            <property name="ErrorCategory" value="Business Rule Violation"/>
            <sequence key="CustomerFaultSeq"/>
            <drop/>
        </then>
    </filter>
</sequence>
